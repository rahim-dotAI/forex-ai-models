name: Run AI Forex Brain Pipeline

on:
  push:
    paths:
      - 'colab_trigger.txt'
    branches:
      - main
  workflow_dispatch:

jobs:
  run-forex-brain:
    runs-on: ubuntu-latest
    env:
      FOREX_PAT: ${{ secrets.FOREX_PAT }}
      BROWSERLESS_TOKEN: ${{ secrets.BROWSERLESS_TOKEN }}
      ALPHA_VANTAGE_KEY: ${{ secrets.ALPHA_VANTAGE_KEY }}
      GIT_USER_NAME: "Forex AI Bot"
      GIT_USER_EMAIL: "nakatonabira3@gmail.com"
      GITHUB_USERNAME: "rahim-dotAI"
      GITHUB_REPO: "forex-ai-models"
      PYTHONIOENCODING: utf-8
      PYTHONUNBUFFERED: 1

    steps:
      # 0️⃣ Pre-clean any broken submodules
      - name: Remove broken submodules before checkout
        run: |
          echo "🧹 Cleaning any broken .gitmodules or nested repos..."
          rm -f .gitmodules || true
          rm -rf forex_ai_outputs/forex-ai-models || true
          echo "✅ Cleaned potential submodule conflicts"

      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
          lfs: false
          submodules: false
          clean: true
          set-safe-directory: true

      # 2️⃣ Verify required secrets
      - name: Check required secrets
        run: |
          if [ -z "${FOREX_PAT}" ]; then
            echo "❌ FOREX_PAT secret is missing!"
            exit 1
          fi
          if [ -z "${BROWSERLESS_TOKEN}" ]; then
            echo "⚠️ BROWSERLESS_TOKEN secret is missing. Live prices will be skipped."
          fi
          if [ -z "${ALPHA_VANTAGE_KEY}" ]; then
            echo "⚠️ ALPHA_VANTAGE_KEY secret is missing. Alpha Vantage data fetch will fail."
          fi

      # 3️⃣ Set up Python 3.11
      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
          cache: 'pip'

      # 4️⃣ Upgrade pip & install dependencies
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip wheel setuptools
          pip install mplfinance firebase-admin dropbox requests beautifulsoup4 \
            pandas numpy ta yfinance pyppeteer nest_asyncio lightgbm joblib \
            matplotlib alpha_vantage tqdm scikit-learn river jupyter

      # 5️⃣ Create working directories (including Colab-style paths)
      - name: Create working directories
        run: |
          # Create standard working directories
          mkdir -p /home/runner/work/forex-ai-models
          mkdir -p forex-alpha-models/csvs
          mkdir -p forex-alpha-models/pickles
          mkdir -p forex-alpha-models/logs
          mkdir -p forex-alpha-models/merged_data_pickles
          mkdir -p forex-alpha-models/temp_pickles
          
          # Create /content directory structure for Colab compatibility
          sudo mkdir -p /content/forex-automation
          sudo mkdir -p /content/forex-alpha-models/csvs
          sudo mkdir -p /content/forex-alpha-models/pickles
          sudo mkdir -p /content/forex-alpha-models/logs
          sudo mkdir -p /content/forex-alpha-models/merged_data_pickles
          sudo mkdir -p /content/forex-alpha-models/temp_pickles
          sudo mkdir -p /content/forex-alpha-models/forex-ai-models
          
          # Give current user ownership of /content
          sudo chown -R $USER:$USER /content
          sudo chmod -R 755 /content
          
          echo "✅ Working directories created (including Colab paths)"

      # 6️⃣ Configure Git globally
      - name: Configure Git
        run: |
          git config --global user.name "${GIT_USER_NAME}"
          git config --global user.email "${GIT_USER_EMAIL}"
          git config --global advice.detachedHead false
          git config --global credential.helper store
          echo "https://${GITHUB_USERNAME}:${FOREX_PAT}@github.com" > ~/.git-credentials
          echo "✅ Git configured with credentials"

      # 7️⃣ Convert notebook to Python script and clean
      - name: Convert notebook to Python script
        run: |
          echo "📝 Converting notebook to Python script..."
          jupyter nbconvert --to python "AI_Forex_Brain_2.ipynb" --output ai_forex_brain_2
          
          echo "🧹 Removing Jupyter-specific commands..."
          # Remove get_ipython() calls (like !pip install)
          sed -i '/get_ipython()/d' ai_forex_brain_2.py
          
          # Remove any remaining IPython magic commands
          sed -i '/^get_ipython/d' ai_forex_brain_2.py
          
          echo "✅ Notebook converted and cleaned successfully"

      # 8️⃣ Execute AI Forex Brain Python Script
      - name: Execute AI Forex Brain Pipeline
        env:
          GITHUB_ACTIONS: "true"
        run: |
          echo "🚀 Starting AI Forex Brain Pipeline..."
          python ai_forex_brain_2.py
        continue-on-error: false
        timeout-minutes: 120

      # 8️⃣ Upload logs as artifacts
      - name: Upload pipeline logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forex-pipeline-logs
          path: |
            forex-alpha-models/logs/*.log
            forex_pipeline.log
          retention-days: 7

      # 9️⃣ Upload generated pickles as artifacts
      - name: Upload generated pickles
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: forex-pickles
          path: |
            forex-alpha-models/merged_data_pickles/*.pkl
            forex-alpha-models/pickles/*.pkl
          retention-days: 3

      # 🔟 Upload signals JSON
      - name: Upload signals JSON
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: forex-signals
          path: |
            forex-alpha-models/forex-ai-models/broker_signals.json
            forex-alpha-models/forex-ai-models/ensemble_signals.json
            forex-alpha-models/forex-ai-models/latest_signals.json
          retention-days: 7

      # 1️⃣1️⃣ Commit and push results
      - name: Commit and push results
        if: success()
        env:
          GIT_AUTH_TOKEN: ${{ secrets.FOREX_PAT }}
        run: |
          cd forex-alpha-models/forex-ai-models || exit 0
          
          echo "📦 Adding changes to git..."
          git add -A
          
          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "✅ No changes to commit"
            exit 0
          fi
          
          # Commit changes
          git commit -m "🤖 Auto-update: AI Forex Brain Pipeline [$(date '+%Y-%m-%d %H:%M:%S')]" || echo "No changes to commit"
          
          # Set remote URL with token
          git remote set-url origin https://x-access-token:${GIT_AUTH_TOKEN}@github.com/${GITHUB_USERNAME}/${GITHUB_REPO}.git
          
          # Pull latest changes to avoid conflicts
          git pull --rebase origin main || echo "Nothing to pull"
          
          # Push changes
          git push origin main && echo "✅ Changes pushed successfully" || echo "⚠️ Push failed or no changes to push"

      # 1️⃣2️⃣ Cleanup sensitive files
      - name: Cleanup sensitive data
        if: always()
        run: |
          rm -f ~/.git-credentials
          echo "✅ Cleaned up sensitive files"

      # 1️⃣3️⃣ Summary report
      - name: Pipeline Summary
        if: always()
        run: |
          echo "================================================"
          echo "🎯 AI FOREX BRAIN PIPELINE SUMMARY"
          echo "================================================"
          echo "📅 Run Date: $(date)"
          echo "🔧 Python Version: $(python --version)"
          echo "📊 Working Directory: $(pwd)"
          echo "================================================"
          
          if [ -f "forex_pipeline.log" ]; then
            echo "📝 Last 20 log entries:"
            tail -n 20 forex_pipeline.log
          fi
          
          echo "================================================"
          echo "✅ Pipeline execution completed"
          echo "================================================"
