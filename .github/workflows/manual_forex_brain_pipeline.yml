name: Smart Forex Brain Pipeline (Weekday Full / Weekend Tagged)

on:
  workflow_dispatch:
  push:
    paths:
      - 'colab_trigger.txt'
    branches:
      - main
  schedule:
    - cron: '0 */4 * * *'

jobs:
  run-forex-brain:
    runs-on: ubuntu-latest
    timeout-minutes: 55

    env:
      FOREX_PAT: ${{ secrets.FOREX_PAT }}
      BROWSERLESS_TOKEN: ${{ secrets.BROWSERLESS_TOKEN }}
      ALPHA_VANTAGE_KEY: ${{ secrets.ALPHA_VANTAGE_KEY }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      GIT_USER_NAME: "Forex AI Bot"
      GIT_USER_EMAIL: "nakatonabira3@gmail.com"
      GITHUB_USERNAME: "rahim-dotAI"
      GITHUB_REPO: "forex-ai-models"
      PYTHONIOENCODING: utf-8
      PYTHONUNBUFFERED: 1
      GITHUB_ACTIONS: "true"
      SINGLE_RUN_MODE: "true"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
          lfs: true
          token: ${{ secrets.FOREX_PAT }}

      - name: Detect Weekend vs Weekday Mode
        id: detect_mode
        run: |
          DAY_OF_WEEK=$(date +%u)
          
          echo "=========================================="
          echo "🗓️  DAY DETECTION"
          echo "=========================================="
          echo "Current day: $DAY_OF_WEEK (1=Mon, 7=Sun)"
          echo "Date: $(date +'%A, %B %d, %Y %H:%M UTC')"
          
          if [ $DAY_OF_WEEK -eq 6 ] || [ $DAY_OF_WEEK -eq 7 ]; then
            echo "is_weekend=true" >> $GITHUB_OUTPUT
            echo "execution_mode=weekend_tagged_cell" >> $GITHUB_OUTPUT
            echo "🎯 MODE: WEEKEND (Tagged Cell v8.5 Only)"
          else
            echo "is_weekend=false" >> $GITHUB_OUTPUT
            echo "execution_mode=weekday_full_notebook" >> $GITHUB_OUTPUT
            echo "🎯 MODE: WEEKDAY (Full Notebook Execution)"
          fi
          echo "=========================================="

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip wheel setuptools
          pip install --no-cache-dir \
            pandas numpy requests beautifulsoup4 scikit-learn jupyter nbconvert ta nbformat

      - name: Configure Git
        run: |
          git config --global user.name "${GIT_USER_NAME}"
          git config --global user.email "${GIT_USER_EMAIL}"
          git config --global credential.helper store
          echo "https://${GITHUB_USERNAME}:${FOREX_PAT}@github.com" > ~/.git-credentials

      - name: Verify data files exist (WEEKEND ONLY)
        if: steps.detect_mode.outputs.is_weekend == 'true'
        run: |
          echo "=========================================="
          echo "VERIFYING DATA FILES FOR WEEKEND RUN"
          echo "=========================================="
          
          PICKLE_COUNT=$(find . -maxdepth 1 -name "*_2244.pkl" -type f 2>/dev/null | wc -l)
          
          if [ $PICKLE_COUNT -lt 4 ]; then
            echo "❌ ERROR: Only $PICKLE_COUNT/4 pickle files found"
            echo "Weekend mode requires existing data files!"
            echo ""
            echo "Available files:"
            ls -lh *.pkl 2>/dev/null || echo "No pickle files found"
            echo ""
            echo "💡 TIP: Run the workflow on a weekday first to generate data files"
            exit 1
          fi
          
          echo "✅ Found all required data files ($PICKLE_COUNT)"
          echo "Files:"
          ls -lh *_2244.pkl
          echo "=========================================="

      - name: Run Tagged Cell v8.5 Pipeline (WEEKEND ONLY)
        if: steps.detect_mode.outputs.is_weekend == 'true'
        run: |
          echo "=========================================="
          echo "🟢 RUNNING TAGGED CELL v8.5 PIPELINE"
          echo "=========================================="
          echo "Mode: ${{ steps.detect_mode.outputs.execution_mode }}"
          echo ""
          
          # Look for v8.5 pipeline file
          PIPELINE_FILE=""
          if [ -f "forex_pipeline_v851.py" ]; then
            PIPELINE_FILE="forex_pipeline_v851.py"
          elif [ -f "forex_pipeline_v85.py" ]; then
            PIPELINE_FILE="forex_pipeline_v85.py"
          elif [ -f "Ultimate_Forex_Pipeline_v851.py" ]; then
            PIPELINE_FILE="Ultimate_Forex_Pipeline_v851.py"
          fi
          
          if [ -z "$PIPELINE_FILE" ]; then
            echo "❌ ERROR: Could not find v8.5 pipeline file!"
            echo ""
            echo "Looking for one of:"
            echo "  - forex_pipeline_v851.py"
            echo "  - forex_pipeline_v85.py"
            echo "  - Ultimate_Forex_Pipeline_v851.py"
            echo ""
            echo "Available Python files:"
            ls -lh *.py 2>/dev/null || echo "No Python files found"
            exit 1
          fi
          
          echo "✅ Found: $PIPELINE_FILE"
          echo ""
          python "$PIPELINE_FILE" 2>&1 | tee pipeline_output.log
          
          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -ne 0 ]; then
            echo ""
            echo "❌ Pipeline failed with exit code $EXIT_CODE"
            echo ""
            echo "Last 100 lines of output:"
            tail -n 100 pipeline_output.log
            exit $EXIT_CODE
          fi
          
          echo ""
          echo "✅ Pipeline completed successfully"
        timeout-minutes: 25

      - name: Run Full Notebook (WEEKDAY ONLY - Monday to Friday)
        if: steps.detect_mode.outputs.is_weekend == 'false'
        run: |
          echo "=========================================="
          echo "📓 RUNNING FULL NOTEBOOK"
          echo "=========================================="
          echo "Mode: ${{ steps.detect_mode.outputs.execution_mode }}"
          echo "Day: $(date +'%A')"
          echo ""
          
          # Check if notebook exists
          NOTEBOOK_FILE=""
          if [ -f "AI_Forex_Brain_2.ipynb" ]; then
            NOTEBOOK_FILE="AI_Forex_Brain_2.ipynb"
          elif [ -f "AI_Forex_Brain.ipynb" ]; then
            NOTEBOOK_FILE="AI_Forex_Brain.ipynb"
          elif [ -f "forex_brain.ipynb" ]; then
            NOTEBOOK_FILE="forex_brain.ipynb"
          fi
          
          if [ -z "$NOTEBOOK_FILE" ]; then
            echo "❌ ERROR: No notebook file found!"
            echo ""
            echo "Looking for one of:"
            echo "  - AI_Forex_Brain_2.ipynb"
            echo "  - AI_Forex_Brain.ipynb"
            echo "  - forex_brain.ipynb"
            echo ""
            echo "Available notebooks:"
            ls -lh *.ipynb 2>/dev/null || echo "No Jupyter notebooks found"
            exit 1
          fi
          
          echo "✅ Found: $NOTEBOOK_FILE"
          echo "📊 Notebook size: $(wc -c < "$NOTEBOOK_FILE") bytes"
          echo ""
          
          # Create Python script to convert and execute notebook
          cat > run_full_notebook.py << 'EOFPYTHON'
          import nbformat
          from nbconvert.preprocessors import ExecutePreprocessor, CellExecutionError
          import sys
          import os
          from pathlib import Path
          
          def run_notebook(notebook_path):
              """Execute entire Jupyter notebook"""
              print(f"📖 Reading notebook: {notebook_path}")
              
              # Read the notebook
              with open(notebook_path, 'r', encoding='utf-8') as f:
                  nb = nbformat.read(f, as_version=4)
              
              print(f"✅ Loaded {len(nb.cells)} cells")
              print(f"   - Code cells: {sum(1 for c in nb.cells if c.cell_type == 'code')}")
              print(f"   - Markdown cells: {sum(1 for c in nb.cells if c.cell_type == 'markdown')}")
              print()
              
              # Configure executor
              ep = ExecutePreprocessor(
                  timeout=2400,  # 40 minutes per cell
                  kernel_name='python3',
                  allow_errors=False  # Stop on first error
              )
              
              print("🚀 Executing notebook...")
              print("=" * 60)
              
              try:
                  # Execute the notebook
                  ep.preprocess(nb, {'metadata': {'path': '.'}})
                  
                  print()
                  print("=" * 60)
                  print("✅ Notebook execution completed successfully!")
                  
                  # Save executed notebook
                  output_path = notebook_path.replace('.ipynb', '_executed.ipynb')
                  with open(output_path, 'w', encoding='utf-8') as f:
                      nbformat.write(nb, f)
                  print(f"💾 Saved executed notebook: {output_path}")
                  
                  return True
                  
              except CellExecutionError as e:
                  print()
                  print("=" * 60)
                  print(f"❌ ERROR: Cell execution failed!")
                  print(f"Cell index: {e.cell_index if hasattr(e, 'cell_index') else 'unknown'}")
                  print(f"Error: {str(e)}")
                  print("=" * 60)
                  return False
              
              except Exception as e:
                  print()
                  print("=" * 60)
                  print(f"❌ ERROR: Unexpected error during execution!")
                  print(f"Error type: {type(e).__name__}")
                  print(f"Error: {str(e)}")
                  print("=" * 60)
                  return False
          
          if __name__ == "__main__":
              notebook = sys.argv[1] if len(sys.argv) > 1 else "AI_Forex_Brain_2.ipynb"
              
              if not os.path.exists(notebook):
                  print(f"❌ ERROR: Notebook not found: {notebook}")
                  sys.exit(1)
              
              success = run_notebook(notebook)
              sys.exit(0 if success else 1)
          EOFPYTHON
          
          echo "🔧 Created notebook executor script"
          echo ""
          
          # Execute the notebook
          python run_full_notebook.py "$NOTEBOOK_FILE" 2>&1 | tee notebook_execution.log
          
          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -ne 0 ]; then
            echo ""
            echo "❌ Notebook execution failed with exit code $EXIT_CODE"
            echo ""
            echo "Last 200 lines of output:"
            tail -n 200 notebook_execution.log
            exit $EXIT_CODE
          fi
          
          echo ""
          echo "✅ Full notebook executed successfully"
        timeout-minutes: 50

      - name: Verify output files
        if: always()
        run: |
          echo "=========================================="
          echo "📋 CHECKING OUTPUT FILES"
          echo "=========================================="
          
          # Check for pickle files
          echo "Pickle files:"
          if ls *_2244.pkl 1> /dev/null 2>&1; then
            ls -lh *_2244.pkl
          else
            echo "  ⚠️  No pickle files found"
          fi
          echo ""
          
          # Check for signal files
          echo "Signal files:"
          if [ -f "broker_signals.json" ]; then
            FILE_SIZE=$(wc -c < "broker_signals.json")
            echo "  ✅ broker_signals.json (${FILE_SIZE} bytes)"
          else
            echo "  ⚠️  broker_signals.json not found"
          fi
          
          if [ -f "ensemble_signals.json" ]; then
            FILE_SIZE=$(wc -c < "ensemble_signals.json")
            echo "  ✅ ensemble_signals.json (${FILE_SIZE} bytes)"
          else
            echo "  ⚠️  ensemble_signals.json not found"
          fi
          echo ""
          
          # Check for learning files
          echo "Learning files:"
          for file in learning_v85.pkl iteration_v85.pkl memory_v85.db; do
            if [ -f "$file" ]; then
              FILE_SIZE=$(wc -c < "$file")
              echo "  ✅ $file (${FILE_SIZE} bytes)"
            else
              echo "  ⚠️  $file not found"
            fi
          done
          echo "=========================================="

      - name: Commit and push changes
        if: always()
        run: |
          git add -A
          
          if git diff --cached --quiet; then
            echo "ℹ️  No changes to commit"
          else
            MODE="${{ steps.detect_mode.outputs.execution_mode }}"
            TIMESTAMP=$(date +'%Y-%m-%d %H:%M:%S UTC')
            DAY=$(date +'%A')
            
            git commit -m "🤖 Auto-update [$MODE] - $DAY: $TIMESTAMP" || true
            
            echo "Syncing with remote..."
            for i in {1..3}; do
              echo "Attempt $i: Pulling latest changes..."
              if git pull --rebase origin main 2>&1 | grep -q "CONFLICT"; then
                echo "⚠️  Conflict detected, resolving with ours strategy..."
                git checkout --ours .
                git add -A
                git rebase --continue || true
              fi
              
              echo "Attempt $i: Pushing to GitHub..."
              if git push origin main 2>&1; then
                echo "✅ Successfully pushed on attempt $i"
                break
              else
                echo "⚠️  Push failed on attempt $i"
                if [ $i -lt 3 ]; then
                  echo "Waiting 3 seconds before retry..."
                  sleep 3
                  git fetch origin main
                  git reset --soft origin/main
                  git add -A
                  git commit -m "🤖 Auto-update [$MODE] - $DAY: $TIMESTAMP" || true
                else
                  echo "❌ All push attempts failed"
                  exit 1
                fi
              fi
            done
          fi

      - name: Upload logs and artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pipeline-logs-${{ github.run_number }}
          path: |
            pipeline_output.log
            notebook_execution.log
            *_executed.ipynb
            run_full_notebook.py
            forex_pipeline_v85.log
            pipeline.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Summary
        if: always()
        run: |
          echo "=========================================="
          echo "📊 EXECUTION SUMMARY"
          echo "=========================================="
          echo "Mode: ${{ steps.detect_mode.outputs.execution_mode }}"
          echo "Weekend: ${{ steps.detect_mode.outputs.is_weekend }}"
          echo "Day: $(date +'%A')"
          echo "Date: $(date +'%B %d, %Y %H:%M UTC')"
          echo ""
          echo "Generated Files:"
          
          # Check each file individually
          if [ -f "broker_signals.json" ]; then
            FILE_SIZE=$(wc -c < "broker_signals.json" 2>/dev/null || echo "0")
            echo "  ✅ broker_signals.json (${FILE_SIZE} bytes)"
          fi
          
          if [ -f "ensemble_signals.json" ]; then
            FILE_SIZE=$(wc -c < "ensemble_signals.json" 2>/dev/null || echo "0")
            echo "  ✅ ensemble_signals.json (${FILE_SIZE} bytes)"
          fi
          
          if [ -f "learning_v85.pkl" ]; then
            FILE_SIZE=$(wc -c < "learning_v85.pkl" 2>/dev/null || echo "0")
            echo "  ✅ learning_v85.pkl (${FILE_SIZE} bytes)"
          fi
          
          if [ -f "iteration_v85.pkl" ]; then
            FILE_SIZE=$(wc -c < "iteration_v85.pkl" 2>/dev/null || echo "0")
            echo "  ✅ iteration_v85.pkl (${FILE_SIZE} bytes)"
          fi
          
          if [ -f "memory_v85.db" ]; then
            FILE_SIZE=$(wc -c < "memory_v85.db" 2>/dev/null || echo "0")
            echo "  ✅ memory_v85.db (${FILE_SIZE} bytes)"
          fi
          
          # Check pickle files
          PICKLE_COUNT=$(find . -maxdepth 1 -name "*_2244.pkl" -type f 2>/dev/null | wc -l)
          if [ $PICKLE_COUNT -gt 0 ]; then
            echo "  ✅ ${PICKLE_COUNT} pickle data files"
          fi
          
          echo "=========================================="
