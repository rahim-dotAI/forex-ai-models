name: Smart Forex Brain Pipeline (Adaptive Schedule)

on:
  workflow_dispatch:
  push:
    paths:
      - 'colab_trigger.txt'
    branches:
      - main
  schedule:
    # Weekdays (Mon-Fri): Every 4 hours
    - cron: '0 */4 * * 1-5'
    # Weekends (Sat-Sun): Every 1 hour
    - cron: '0 * * * 0,6'

jobs:
  run-forex-brain:
    runs-on: ubuntu-latest
    timeout-minutes: 55

    env:
      FOREX_PAT: ${{ secrets.FOREX_PAT }}
      BROWSERLESS_TOKEN: ${{ secrets.BROWSERLESS_TOKEN }}
      ALPHA_VANTAGE_KEY: ${{ secrets.ALPHA_VANTAGE_KEY }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      GIT_USER_NAME: "Forex AI Bot"
      GIT_USER_EMAIL: "nakatonabira3@gmail.com"
      GITHUB_USERNAME: "rahim-dotAI"
      GITHUB_REPO: "forex-ai-models"
      PYTHONIOENCODING: utf-8
      PYTHONUNBUFFERED: 1
      GITHUB_ACTIONS: "true"
      SINGLE_RUN_MODE: "true"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
          lfs: true
          token: ${{ secrets.FOREX_PAT }}

      - name: Fix submodule issue
        run: |
          echo "=========================================="
          echo "🔧 FIXING SUBMODULE CONFIGURATION"
          echo "=========================================="
          
          if [ -f ".gitmodules" ]; then
            echo "⚠️ Found .gitmodules file - removing it"
            rm -f .gitmodules
            git rm --cached -r forex-alpha-models 2>/dev/null || true
            rm -rf forex-alpha-models
            git add .gitmodules 2>/dev/null || true
            echo "✅ Submodule configuration removed"
          else
            echo "✅ No .gitmodules file found"
          fi
          
          find . -mindepth 2 -name ".git" -type d -exec rm -rf {} + 2>/dev/null || true
          echo "=========================================="

      - name: Detect Weekend vs Weekday Mode
        id: detect_mode
        run: |
          DAY_OF_WEEK=$(date +%u)
          HOUR=$(date +%H)
          
          echo "=========================================="
          echo "🗓️  EXECUTION MODE DETECTION"
          echo "=========================================="
          echo "Current day: $DAY_OF_WEEK (1=Mon, 7=Sun)"
          echo "Current hour: $HOUR UTC"
          echo "Date: $(date +'%A, %B %d, %Y %H:%M UTC')"
          echo ""
          
          if [ $DAY_OF_WEEK -eq 6 ] || [ $DAY_OF_WEEK -eq 7 ]; then
            echo "is_weekend=true" >> $GITHUB_OUTPUT
            echo "execution_mode=weekend_tagged_cells" >> $GITHUB_OUTPUT
            echo "schedule_type=hourly" >> $GITHUB_OUTPUT
            echo "🎯 MODE: WEEKEND"
            echo "   • Execution: Tagged cells only (Omega v12.0 PRODUCTION)"
            echo "   • Schedule: Every 1 hour"
            echo "   • Focus: Learning & optimization with strict validation"
          else
            echo "is_weekend=false" >> $GITHUB_OUTPUT
            echo "execution_mode=weekday_full_notebook" >> $GITHUB_OUTPUT
            echo "schedule_type=4hourly" >> $GITHUB_OUTPUT
            echo "🎯 MODE: WEEKDAY"
            echo "   • Execution: Full notebook (all cells)"
            echo "   • Schedule: Every 4 hours"
            echo "   • Focus: Live trading + data collection"
          fi
          echo "=========================================="

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          echo "📦 Installing Python dependencies..."
          python -m pip install --upgrade pip wheel setuptools
          pip install --no-cache-dir \
            pandas numpy requests beautifulsoup4 scikit-learn \
            jupyter nbconvert nbformat ta yfinance \
            mplfinance firebase-admin dropbox \
            pyppeteer nest_asyncio lightgbm joblib matplotlib \
            alpha_vantage tqdm river scipy
          echo "✅ Dependencies installed"

      - name: Configure Git
        run: |
          git config --global user.name "${GIT_USER_NAME}"
          git config --global user.email "${GIT_USER_EMAIL}"
          git config --global credential.helper store
          echo "https://${GITHUB_USERNAME}:${FOREX_PAT}@github.com" > ~/.git-credentials
          echo "✅ Git configured"

      - name: Check for Required Data Files (Weekend)
        if: steps.detect_mode.outputs.is_weekend == 'true'
        id: check_data
        run: |
          echo "=========================================="
          echo "🔍 CHECKING FOR REQUIRED DATA FILES"
          echo "=========================================="
          
          # Check for processed pickle files (excluding model and cache files)
          PICKLE_COUNT=$(find data/processed -name "*.pkl" -type f 2>/dev/null | \
            grep -v "_sgd_model\|_rf_model\|indicator_cache\|ultra_\|alpha_\|_model.pkl" | wc -l)
          
          # Check for database file
          DB_EXISTS=false
          if [ -f "database/memory_v85.db" ]; then
            DB_SIZE=$(wc -c < "database/memory_v85.db" 2>/dev/null || echo "0")
            if [ "$DB_SIZE" -gt "1000" ]; then
              DB_EXISTS=true
            fi
          fi
          
          # Check for Omega state files
          OMEGA_STATE_EXISTS=false
          if [ -f "omega_state/omega_learning.pkl" ] || [ -f "omega_state/omega_iteration.pkl" ]; then
            OMEGA_STATE_EXISTS=true
          fi
          
          echo "📊 Data Status:"
          echo "   Processed pickle files: $PICKLE_COUNT"
          echo "   Database exists: $DB_EXISTS"
          echo "   Omega state exists: $OMEGA_STATE_EXISTS"
          echo ""
          
          # Decision: need pickles AND database for tagged cells
          if [ $PICKLE_COUNT -ge 4 ] && [ "$DB_EXISTS" = "true" ]; then
            echo "has_data=true" >> $GITHUB_OUTPUT
            echo "execution_type=tagged_cells" >> $GITHUB_OUTPUT
            echo "✅ SUFFICIENT DATA FOUND"
            echo "   • Will run: Tagged cells only (Omega v12.0 PRODUCTION)"
            echo "   • Pickle files: $PICKLE_COUNT/4 ✓"
            echo "   • Database: Present ✓"
            echo "   • Mode: Fast learning with strict validation (2-5 min)"
          else
            echo "has_data=false" >> $GITHUB_OUTPUT
            echo "execution_type=full_notebook" >> $GITHUB_OUTPUT
            echo "⚠️  INSUFFICIENT DATA - NEEDS FULL NOTEBOOK"
            echo "   • Pickle files: $PICKLE_COUNT/4 (need ≥4)"
            echo "   • Database: $([ "$DB_EXISTS" = "true" ] && echo "Present ✓" || echo "Missing ✗")"
            echo "   • Will run: Full notebook to generate all data"
            echo "   • Purpose: Create foundation for future runs"
            echo "   • Time: ~40-50 minutes"
            echo "   • Next weekend: Will use fast tagged cells!"
          fi
          echo "=========================================="

      - name: Run Weekend Tagged Cells (Omega v12.0 PRODUCTION)
        if: steps.detect_mode.outputs.is_weekend == 'true' && steps.check_data.outputs.has_data == 'true'
        run: |
          echo "=========================================="
          echo "🎯 WEEKEND MODE: OMEGA v12.0 PRODUCTION"
          echo "=========================================="
          echo "Executing: Tagged pipeline_main cells only"
          echo "Data available: Yes (proceeding with learning mode)"
          echo ""
          
          # Create Python script to extract and run tagged cells
          cat > run_tagged_cells.py << 'EOFPYTHON'
          import nbformat
          import sys
          import os
          from datetime import datetime
          
          def extract_and_run_tagged_cells(notebook_path):
              """Extract and execute cells with TAG: pipeline_main or #TAG: pipeline_main"""
              print(f"📖 Reading notebook: {notebook_path}")
              
              with open(notebook_path, 'r', encoding='utf-8') as f:
                  nb = nbformat.read(f, as_version=4)
              
              tagged_cells = []
              cell_numbers = []
              
              # Find cells with TAG: pipeline_main (with or without space after #)
              for i, cell in enumerate(nb.cells):
                  if cell.cell_type == 'code':
                      source = cell.source
                      # Check for both "# TAG:" and "#TAG:"
                      if '# TAG: pipeline_main' in source or '#TAG: pipeline_main' in source or '# TAG:pipeline_main' in source:
                          tagged_cells.append(source)
                          cell_numbers.append(i + 1)
              
              if not tagged_cells:
                  print("❌ No tagged cells found with 'TAG: pipeline_main'")
                  return False
              
              print(f"✅ Found {len(tagged_cells)} tagged cell(s): {cell_numbers}")
              print("=" * 70)
              print()
              
              # Execute each tagged cell
              for idx, (cell_num, code) in enumerate(zip(cell_numbers, tagged_cells), 1):
                  print(f"🔄 Executing tagged cell {idx}/{len(tagged_cells)} (cell #{cell_num})")
                  print("-" * 70)
                  
                  # Remove the TAG comment before execution (handle multiple formats)
                  code_lines = [line for line in code.split('\n') 
                               if not (line.strip().startswith('# TAG:') or 
                                      line.strip().startswith('#TAG:'))]
                  clean_code = '\n'.join(code_lines)
                  
                  try:
                      # Execute the code
                      exec(clean_code, globals())
                      print("-" * 70)
                      print(f"✅ Cell {idx} completed successfully")
                      print("=" * 70)
                      print()
                  except Exception as e:
                      print("-" * 70)
                      print(f"❌ Cell {idx} failed with error:")
                      print(f"   {type(e).__name__}: {str(e)}")
                      import traceback
                      traceback.print_exc()
                      print("=" * 70)
                      return False
              
              print()
              print("=" * 70)
              print("✅ ALL TAGGED CELLS EXECUTED SUCCESSFULLY")
              print("=" * 70)
              return True
          
          if __name__ == "__main__":
              notebook = sys.argv[1] if len(sys.argv) > 1 else "AI_Forex_Brain_2.ipynb"
              
              if not os.path.exists(notebook):
                  print(f"❌ ERROR: Notebook not found: {notebook}")
                  sys.exit(1)
              
              print("=" * 70)
              print("🎯 WEEKEND MODE - OMEGA v12.0 PRODUCTION")
              print("=" * 70)
              print(f"📅 Started: {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print(f"📓 Notebook: {notebook}")
              print(f"🎯 Target: Cells with 'TAG: pipeline_main'")
              print("🔴 Version: Omega v12.0 - PRODUCTION READY")
              print("=" * 70)
              print()
              
              success = extract_and_run_tagged_cells(notebook)
              
              print()
              print("=" * 70)
              if success:
                  print("✅ WEEKEND EXECUTION COMPLETED")
              else:
                  print("❌ WEEKEND EXECUTION FAILED")
              print(f"📅 Finished: {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print("=" * 70)
              
              sys.exit(0 if success else 1)
          EOFPYTHON
          
          # Execute tagged cells
          python run_tagged_cells.py "AI_Forex_Brain_2.ipynb" 2>&1 | tee weekend_execution.log
          
          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -ne 0 ]; then
            echo ""
            echo "❌ Weekend execution failed with exit code $EXIT_CODE"
            tail -n 100 weekend_execution.log
            exit $EXIT_CODE
          fi
          
          echo ""
          echo "✅ Weekend tagged cells executed successfully"
        timeout-minutes: 50

      - name: Run Full Notebook on Weekend (Data Generation)
        if: steps.detect_mode.outputs.is_weekend == 'true' && steps.check_data.outputs.has_data == 'false'
        run: |
          echo "=========================================="
          echo "📓 WEEKEND MODE: FULL NOTEBOOK EXECUTION"
          echo "=========================================="
          echo "Reason: Insufficient data for tagged cells"
          echo "Purpose: Generate required pickle files and database"
          echo "Next weekend: Will use tagged cells only"
          echo ""
          
          # Create enhanced Python script with cell progress tracking
          cat > run_full_notebook_weekend.py << 'EOFPYTHON'
          import nbformat
          from nbconvert.preprocessors import ExecutePreprocessor, CellExecutionError
          import sys
          import os
          import time
          import re
          from datetime import datetime
          
          class ProgressTrackingExecutor(ExecutePreprocessor):
              """Custom executor that shows progress"""
              
              def __init__(self, *args, **kwargs):
                  super().__init__(*args, **kwargs)
                  self.total_cells = 0
                  self.current_cell = 0
                  self.start_time = None
                  
              def preprocess(self, nb, resources=None, km=None):
                  self.total_cells = sum(1 for cell in nb.cells if cell.cell_type == 'code')
                  self.current_cell = 0
                  self.start_time = time.time()
                  
                  print(f"📊 Total code cells to execute: {self.total_cells}")
                  print("=" * 70)
                  print()
                  
                  return super().preprocess(nb, resources, km)
              
              def preprocess_cell(self, cell, resources, cell_index):
                  if cell.cell_type != 'code':
                      return cell, resources
                  
                  self.current_cell += 1
                  elapsed = time.time() - self.start_time
                  progress_pct = (self.current_cell / self.total_cells) * 100
                  
                  print()
                  print("=" * 70)
                  print(f"🔄 CELL {self.current_cell}/{self.total_cells} ({progress_pct:.1f}%)")
                  print(f"⏱️  Elapsed: {elapsed:.1f}s")
                  
                  if cell.source:
                      first_line = cell.source.split('\n')[0][:60]
                      if first_line.strip() and not first_line.strip().startswith('#'):
                          print(f"📝 {first_line}...")
                  
                  print("-" * 70)
                  
                  cell_start = time.time()
                  cell, resources = super().preprocess_cell(cell, resources, cell_index)
                  cell_duration = time.time() - cell_start
                  
                  if cell.outputs:
                      for output in cell.outputs:
                          if output.output_type == 'stream':
                              text = self._clean_output(output.text)
                              if text.strip():
                                  print(text)
                          elif output.output_type == 'error':
                              print(f"❌ ERROR: {output.ename}: {output.evalue}")
                  
                  print("-" * 70)
                  print(f"✅ Cell {self.current_cell} completed in {cell_duration:.1f}s")
                  print("=" * 70)
                  
                  return cell, resources
              
              def _clean_output(self, text):
                  if not text:
                      return ""
                  lines = []
                  for line in text.split('\n'):
                      if any(skip in line for skip in ['[DEBUG]', 'WARNING:', 'DeprecationWarning']):
                          continue
                      if not line.strip():
                          continue
                      line = re.sub(r'\x1b\[[0-9;]*m', '', line)
                      lines.append(line)
                  return '\n'.join(lines)
          
          def run_notebook(notebook_path):
              print(f"📖 Reading: {notebook_path}")
              
              with open(notebook_path, 'r', encoding='utf-8') as f:
                  nb = nbformat.read(f, as_version=4)
              
              code_cells = sum(1 for c in nb.cells if c.cell_type == 'code')
              print(f"✅ Loaded: {len(nb.cells)} cells ({code_cells} code)")
              print()
              
              ep = ProgressTrackingExecutor(
                  timeout=2400,
                  kernel_name='python3',
                  allow_errors=False
              )
              
              print("🚀 Starting full notebook execution (data generation)...")
              print()
              
              try:
                  start_time = time.time()
                  ep.preprocess(nb, {'metadata': {'path': '.'}})
                  total_duration = time.time() - start_time
                  
                  print()
                  print("=" * 70)
                  print("✅ FULL NOTEBOOK COMPLETED!")
                  print("=" * 70)
                  print(f"⏱️  Time: {total_duration:.1f}s ({total_duration/60:.1f} min)")
                  print(f"📊 Cells: {code_cells}")
                  print("=" * 70)
                  
                  return True
                  
              except Exception as e:
                  print()
                  print("=" * 70)
                  print("❌ EXECUTION FAILED!")
                  print("=" * 70)
                  print(f"Error: {type(e).__name__}: {str(e)}")
                  print("=" * 70)
                  return False
          
          if __name__ == "__main__":
              notebook = sys.argv[1] if len(sys.argv) > 1 else "AI_Forex_Brain_2.ipynb"
              
              if not os.path.exists(notebook):
                  print(f"❌ Notebook not found: {notebook}")
                  sys.exit(1)
              
              print("=" * 70)
              print("📓 WEEKEND DATA GENERATION MODE")
              print("=" * 70)
              print(f"📅 {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print(f"📓 {notebook}")
              print("🎯 Purpose: Create data files for future learning runs")
              print("=" * 70)
              print()
              
              success = run_notebook(notebook)
              
              print()
              print("=" * 70)
              if success:
                  print("✅ DATA GENERATION COMPLETED")
                  print("💡 Next weekend: Will use tagged cells only!")
              else:
                  print("❌ DATA GENERATION FAILED")
              print(f"📅 {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print("=" * 70)
              
              sys.exit(0 if success else 1)
          EOFPYTHON
          
          # Execute notebook
          python run_full_notebook_weekend.py "AI_Forex_Brain_2.ipynb" 2>&1 | tee weekend_data_generation.log
          
          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -ne 0 ]; then
            echo "❌ Data generation failed with exit code $EXIT_CODE"
            tail -n 100 weekend_data_generation.log
            exit $EXIT_CODE
          fi
          
          echo ""
          echo "✅ Data generation completed - future weekend runs will use tagged cells"
        timeout-minutes: 50

      - name: Run Full Notebook (WEEKDAY - Monday to Friday)
        if: steps.detect_mode.outputs.is_weekend == 'false'
        run: |
          echo "=========================================="
          echo "📓 WEEKDAY MODE: FULL NOTEBOOK EXECUTION"
          echo "=========================================="
          echo "Day: $(date +'%A')"
          echo "Executing: All cells in sequence"
          echo ""
          
          # Create Python script with progress tracking
          cat > run_full_notebook.py << 'EOFPYTHON'
          import nbformat
          from nbconvert.preprocessors import ExecutePreprocessor, CellExecutionError
          import sys
          import os
          import time
          import re
          from datetime import datetime
          
          class ProgressTrackingExecutor(ExecutePreprocessor):
              """Custom executor with progress tracking"""
              
              def __init__(self, *args, **kwargs):
                  super().__init__(*args, **kwargs)
                  self.total_cells = 0
                  self.current_cell = 0
                  self.start_time = None
                  
              def preprocess(self, nb, resources=None, km=None):
                  self.total_cells = sum(1 for cell in nb.cells if cell.cell_type == 'code')
                  self.current_cell = 0
                  self.start_time = time.time()
                  
                  print(f"📊 Total code cells: {self.total_cells}")
                  print("=" * 70)
                  
                  return super().preprocess(nb, resources, km)
              
              def preprocess_cell(self, cell, resources, cell_index):
                  if cell.cell_type != 'code':
                      return cell, resources
                  
                  self.current_cell += 1
                  elapsed = time.time() - self.start_time
                  progress = (self.current_cell / self.total_cells) * 100
                  
                  print(f"\n🔄 Cell {self.current_cell}/{self.total_cells} ({progress:.1f}%) | ⏱️ {elapsed:.1f}s")
                  
                  cell_start = time.time()
                  cell, resources = super().preprocess_cell(cell, resources, cell_index)
                  cell_time = time.time() - cell_start
                  
                  if cell.outputs:
                      for output in cell.outputs:
                          if output.output_type == 'stream':
                              print(self._clean_output(output.text))
                          elif output.output_type == 'error':
                              print(f"❌ {output.ename}: {output.evalue}")
                  
                  print(f"✅ Completed in {cell_time:.1f}s")
                  
                  return cell, resources
              
              def _clean_output(self, text):
                  lines = [re.sub(r'\x1b\[[0-9;]*m', '', line) 
                          for line in text.split('\n')
                          if line.strip() and not any(x in line for x in ['[DEBUG]', 'WARNING:'])]
                  return '\n'.join(lines)
          
          def run_notebook(notebook_path):
              with open(notebook_path, 'r', encoding='utf-8') as f:
                  nb = nbformat.read(f, as_version=4)
              
              ep = ProgressTrackingExecutor(timeout=2400, kernel_name='python3', allow_errors=False)
              
              try:
                  start = time.time()
                  ep.preprocess(nb, {'metadata': {'path': '.'}})
                  duration = time.time() - start
                  
                  print(f"\n✅ Completed in {duration/60:.1f} min")
                  return True
              except Exception as e:
                  print(f"\n❌ Failed: {e}")
                  return False
          
          if __name__ == "__main__":
              notebook = sys.argv[1] if len(sys.argv) > 1 else "AI_Forex_Brain_2.ipynb"
              
              if not os.path.exists(notebook):
                  print(f"❌ Not found: {notebook}")
                  sys.exit(1)
              
              print("=" * 70)
              print("🧠 WEEKDAY FULL NOTEBOOK")
              print(f"📅 {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print("=" * 70)
              
              success = run_notebook(notebook)
              sys.exit(0 if success else 1)
          EOFPYTHON
          
          # Execute notebook
          python run_full_notebook.py "AI_Forex_Brain_2.ipynb" 2>&1 | tee notebook_execution.log
          
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            tail -n 200 notebook_execution.log
            exit 1
          fi
        timeout-minutes: 50

      - name: Verify output files
        if: always()
        run: |
          echo "=========================================="
          echo "📋 CHECKING OUTPUT FILES"
          echo "=========================================="
          
          if [ "${{ steps.detect_mode.outputs.is_weekend }}" = "true" ]; then
            if [ "${{ steps.check_data.outputs.has_data }}" = "true" ]; then
              echo "Mode: Weekend Tagged (Omega v12.0 PRODUCTION)"
            else
              echo "Mode: Weekend Full (Data Gen)"
            fi
          else
            echo "Mode: Weekday Full"
          fi
          echo ""
          
          for dir in outputs data/processed database logs omega_state; do
            if [ -d "$dir" ]; then
              echo "  ✅ $dir/ ($(find "$dir" -type f 2>/dev/null | wc -l) files)"
            else
              echo "  ⚠️  $dir/ not found"
            fi
          done
          
          echo ""
          for file in outputs/latest_signals.json outputs/omega_signals.json; do
            [ -f "$file" ] && echo "  ✅ $file ($(wc -c < "$file") bytes)" || echo "  ⚠️  $file missing"
          done
          
          PICKLE_COUNT=$(find data/processed -name "*.pkl" -type f 2>/dev/null | \
            grep -v "_model\|cache" | wc -l)
          echo ""
          echo "  ✅ $PICKLE_COUNT processed pickle files"
          echo "=========================================="

      - name: Clean up nested repositories
        if: always()
        run: |
          find . -mindepth 2 -type d -name ".git" -exec rm -rf {} + 2>/dev/null || true
          [ -d "forex-alpha-models" ] && rm -rf forex-alpha-models
          echo "✅ Cleanup complete"

      - name: Commit and push changes
        if: always()
        run: |
          git add -A
          
          if git diff --cached --quiet; then
            echo "ℹ️  No changes"
          else
            MODE="${{ steps.detect_mode.outputs.execution_mode }}"
            SCHEDULE="${{ steps.detect_mode.outputs.schedule_type }}"
            TIMESTAMP=$(date +'%Y-%m-%d %H:%M UTC')
            DAY=$(date +'%A')
            
            if [ "${{ steps.detect_mode.outputs.is_weekend }}" = "true" ]; then
              if [ "${{ steps.check_data.outputs.has_data }}" = "true" ]; then
                COMMIT_MSG="🔴 Omega v12.0 PRODUCTION [$SCHEDULE] - $DAY: $TIMESTAMP"
              else
                COMMIT_MSG="📓 Weekend Data Gen [$SCHEDULE] - $DAY: $TIMESTAMP"
              fi
            else
              COMMIT_MSG="🤖 Weekday Trading [$SCHEDULE] - $DAY: $TIMESTAMP"
            fi
            
            git commit -m "$COMMIT_MSG" || true
            
            echo "Syncing with remote..."
            for i in {1..3}; do
              echo "Attempt $i: Pulling..."
              if git pull --rebase origin main 2>&1 | grep -q "CONFLICT"; then
                echo "⚠️  Resolving conflict..."
                git checkout --ours .
                git add -A
                git rebase --continue || true
              fi
              
              echo "Attempt $i: Pushing..."
              if git push origin main 2>&1; then
                echo "✅ Pushed on attempt $i"
                break
              else
                echo "⚠️  Failed attempt $i"
                if [ $i -lt 3 ]; then
                  sleep 3
                  git fetch origin main
                  git reset --soft origin/main
                  git add -A
                  git commit -m "$COMMIT_MSG" || true
                fi
              fi
            done
          fi

      - name: Upload logs and artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: execution-logs-${{ github.run_number }}
          path: |
            weekend_execution.log
            weekend_data_generation.log
            notebook_execution.log
            run_tagged_cells.py
            run_full_notebook.py
            run_full_notebook_weekend.py
            logs/*.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Execution Summary
        if: always()
        run: |
          echo "=========================================="
          echo "📊 EXECUTION SUMMARY"
          echo "=========================================="
          echo "Mode: ${{ steps.detect_mode.outputs.execution_mode }}"
          echo "Schedule: ${{ steps.detect_mode.outputs.schedule_type }}"
          echo "Weekend: ${{ steps.detect_mode.outputs.is_weekend }}"
          echo "Day: $(date +'%A')"
          echo "Time: $(date +'%B %d, %Y %H:%M UTC')"
          echo ""
          
          if [ "${{ steps.detect_mode.outputs.is_weekend }}" = "true" ]; then
            echo "🎯 Weekend Mode:"
            
            if [ "${{ steps.check_data.outputs.has_data }}" = "true" ]; then
              echo "

✅ Executed: Omega v12.0 PRODUCTION (tagged cells)"
              echo "   • Version: Production Ready with Strict Validation"
              echo "   • Features: Hard stops + 50% min validation + 5-fold CV"
              echo "   • Data: Sufficient (fast mode)"
              echo "   • Time: ~2-5 minutes"
              echo "   • Mode: Learning & optimization"
              echo "   • Anti-overfit: ✅ Strict (min 50% val, 0.75 ratio, max 500 samples)"
            elif [ "${{ steps.check_data.outputs.has_data }}" = "false" ]; then
              echo "   ✅ Executed: Full notebook (data generation)"
              echo "   • Data: Insufficient - generated files"
              echo "   • Time: ~40-50 minutes"
              echo "   • Mode: Foundation building"
              echo "   • Next run: Will use Omega v12.0!"
            else
              echo "   ⚠️  No execution - check logs"
            fi
            
            echo "   • Next run: In ~1 hour"
          else
            echo "🤖 Weekday Mode:"
            echo "   • Executed: Full notebook (all cells)"
            echo "   • Pipeline: API Keys → Environment → GitHub Sync"
            echo "   • Data: Alpha Vantage + YFinance fetch"
            echo "   • Processing: CSV Combiner with indicators"
            echo "   • ML: Omega v12.0 PRODUCTION"
            echo "   • Next run: In ~4 hours"
          fi
          echo ""
          
          echo "Generated Files:"
          find outputs -type f -exec sh -c 'echo "  ✅ $(basename {}) ($(wc -c < {} 2>/dev/null || echo 0) bytes)"' \; 2>/dev/null | head -10
          echo ""
          
          PICKLE_COUNT=$(find data/processed -name "*.pkl" -type f 2>/dev/null | \
            grep -v "_sgd_model\|_rf_model\|indicator_cache\|ultra_\|alpha_\|_model.pkl" | wc -l)
          
          echo "📦 Data Files:"
          echo "   • Processed pickles: $PICKLE_COUNT"
          
          if [ -f "database/memory_v85.db" ]; then
            DB_SIZE=$(wc -c < "database/memory_v85.db" 2>/dev/null || echo "0")
            echo "   • Database: ✅ (${DB_SIZE} bytes)"
          else
            echo "   • Database: ⚠️  Not found"
          fi
          
          if [ $PICKLE_COUNT -ge 4 ] && [ -f "database/memory_v85.db" ]; then
            echo "   • Weekend Mode: ✅ Ready for Omega v12.0"
          else
            echo "   • Weekend Mode: ⚠️  Needs data generation (≥4 pickles + DB)"
          fi
          echo ""
          
          echo "🔴 Omega v12.0 Features Active:"
          echo "   CORE IMPROVEMENTS:"
          echo "   • 🛡️ Strict anti-overfit enforcement (hard stops)"
          echo "   • 📊 Realistic validation (min 50% accuracy)"
          echo "   • 🎯 Simplified signal (2 components vs 4)"
          echo "   • ⚡ Stronger regularization (mutation=0.35)"
          echo ""
          echo "   VALIDATION REQUIREMENTS:"
          echo "   • 🛑 Hard minimum: 50% validation accuracy"
          echo "   • 🛑 Val/Train ratio: ≥0.75 (75%)"
          echo "   • 📊 5-fold cross-validation for robustness"
          echo "   • 📉 Max training samples: 500 (prevent overfitting)"
          echo "   • 📏 Temporal gap: 10% (vs 5% before)"
          echo ""
          echo "   SAFETY FEATURES:"
          echo "   • ✅ Paper trading mode by default"
          echo "   • ⚠️ No signals if all models fail validation"
          echo "   • 🔴 Automatic model rejection on failure"
          echo "   • 📝 Full audit trail in logs"
          echo ""
          echo "   CODE QUALITY:"
          echo "   • 🧹 Streamlined (-40% lines)"
          echo "   • 🎯 Production-ready architecture"
          echo "   • 📊 Enhanced logging & monitoring"
          echo "   • ⚡ Optimized performance"
          echo ""
          echo "=========================================="
          echo "✅ Pipeline execution completed"
          echo "=========================================="
