name: Smart Forex Brain Pipeline (Trade Beacon v13.0)

on:
  workflow_dispatch:
  push:
    paths:
      - 'colab_trigger.txt'
    branches:
      - main
  schedule:
    # Weekdays only (Mon-Fri): Every 4 hours
    - cron: '0 */4 * * 1-5'

jobs:
  run-forex-brain:
    runs-on: ubuntu-latest
    timeout-minutes: 55

    env:
      FOREX_PAT: ${{ secrets.FOREX_PAT }}
      BROWSERLESS_TOKEN: ${{ secrets.BROWSERLESS_TOKEN }}
      ALPHA_VANTAGE_KEY: ${{ secrets.ALPHA_VANTAGE_KEY }}
      GMAIL_USER: ${{ secrets.GMAIL_USER }}
      GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
      GIT_USER_NAME: "Forex AI Bot"
      GIT_USER_EMAIL: "nakatonabira3@gmail.com"
      GITHUB_USERNAME: "rahim-dotAI"
      GITHUB_REPO: "forex-ai-models"
      PYTHONIOENCODING: utf-8
      PYTHONUNBUFFERED: 1
      GITHUB_ACTIONS: "true"
      SINGLE_RUN_MODE: "true"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0
          lfs: true
          token: ${{ secrets.FOREX_PAT }}

      - name: Fix submodule issue
        run: |
          echo "=========================================="
          echo "🔧 FIXING SUBMODULE CONFIGURATION"
          echo "=========================================="
          
          if [ -f ".gitmodules" ]; then
            echo "⚠️ Found .gitmodules file - removing it"
            rm -f .gitmodules
            git rm --cached -r forex-alpha-models 2>/dev/null || true
            rm -rf forex-alpha-models
            git add .gitmodules 2>/dev/null || true
            echo "✅ Submodule configuration removed"
          else
            echo "✅ No .gitmodules file found"
          fi
          
          find . -mindepth 2 -name ".git" -type d -exec rm -rf {} + 2>/dev/null || true
          echo "=========================================="

      - name: Detect Execution Mode
        id: detect_mode
        run: |
          DAY_OF_WEEK=$(date +%u)
          HOUR=$(date +%H)
          
          echo "=========================================="
          echo "🗓️  EXECUTION MODE DETECTION"
          echo "=========================================="
          echo "Current day: $DAY_OF_WEEK (1=Mon, 7=Sun)"
          echo "Current hour: $HOUR UTC"
          echo "Date: $(date +'%A, %B %d, %Y %H:%M UTC')"
          echo ""
          
          echo "execution_mode=weekday_full_notebook" >> $GITHUB_OUTPUT
          echo "schedule_type=4hourly" >> $GITHUB_OUTPUT
          echo "🎯 MODE: WEEKDAY (Monday-Friday)"
          echo "   • Execution: Full notebook (all cells)"
          echo "   • Schedule: Every 4 hours"
          echo "   • Features: Data collection + Trade Beacon v13.0"
          echo "   • Trade Beacon: Enhanced with Regime Detection + Session Analysis + Momentum"
          echo "=========================================="

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          echo "📦 Installing Python dependencies..."
          python -m pip install --upgrade pip wheel setuptools
          pip install --no-cache-dir \
            pandas numpy requests beautifulsoup4 scikit-learn \
            jupyter nbconvert nbformat ta yfinance \
            mplfinance firebase-admin dropbox \
            pyppeteer nest_asyncio lightgbm joblib matplotlib \
            alpha_vantage tqdm river scipy
          echo "✅ Dependencies installed"

      - name: Configure Git
        run: |
          git config --global user.name "${GIT_USER_NAME}"
          git config --global user.email "${GIT_USER_EMAIL}"
          git config --global credential.helper store
          echo "https://${GITHUB_USERNAME}:${FOREX_PAT}@github.com" > ~/.git-credentials
          echo "✅ Git configured"

      - name: Check for Required Data Files
        id: check_data
        run: |
          echo "=========================================="
          echo "🔍 CHECKING FOR REQUIRED DATA FILES"
          echo "=========================================="
          
          # Check for processed pickle files (excluding model and cache files)
          PICKLE_COUNT=$(find data/processed -name "*.pkl" -type f 2>/dev/null | \
            grep -v "_sgd_model\|_rf_model\|indicator_cache\|ultra_\|alpha_\|_model.pkl\|\.bak" | wc -l)
          
          # Check for Omega state files
          OMEGA_STATE_EXISTS=false
          if [ -d "omega_state" ]; then
            if [ -f "omega_state/omega_learning.pkl" ] || [ -f "omega_state/omega_iteration.pkl" ]; then
              OMEGA_STATE_EXISTS=true
            fi
          fi
          
          echo "📊 Data Status:"
          echo "   Processed pickle files: $PICKLE_COUNT"
          echo "   Omega state exists: $OMEGA_STATE_EXISTS"
          echo ""
          
          if [ $PICKLE_COUNT -ge 4 ]; then
            echo "has_data=true" >> $GITHUB_OUTPUT
            echo "✅ SUFFICIENT DATA FOUND"
            echo "   • Pickle files: $PICKLE_COUNT/4 ✓"
            echo "   • Ready for Trade Beacon v13.0"
          else
            echo "has_data=false" >> $GITHUB_OUTPUT
            echo "⚠️  INSUFFICIENT DATA"
            echo "   • Pickle files: $PICKLE_COUNT/4 (need ≥4)"
            echo "   • Will generate via full notebook"
          fi
          echo "=========================================="

      - name: Run Full Notebook (Weekday Mode)
        run: |
          echo "=========================================="
          echo "📓 WEEKDAY MODE: FULL NOTEBOOK EXECUTION"
          echo "=========================================="
          echo "Day: $(date +'%A')"
          echo "Executing: All cells in sequence"
          echo "Trade Beacon: v13.0 Enhanced Production"
          echo ""
          
          # Create Python script with progress tracking
          cat > run_full_notebook.py << 'EOFPYTHON'
          import nbformat
          from nbconvert.preprocessors import ExecutePreprocessor
          import sys
          import os
          import time
          import re
          from datetime import datetime
          
          class ProgressTrackingExecutor(ExecutePreprocessor):
              """Custom executor with progress tracking"""
              
              def __init__(self, *args, **kwargs):
                  super().__init__(*args, **kwargs)
                  self.total_cells = 0
                  self.current_cell = 0
                  self.start_time = None
                  
              def preprocess(self, nb, resources=None, km=None):
                  self.total_cells = sum(1 for cell in nb.cells if cell.cell_type == 'code')
                  self.current_cell = 0
                  self.start_time = time.time()
                  
                  print(f"📊 Total code cells: {self.total_cells}")
                  print("=" * 70)
                  
                  return super().preprocess(nb, resources, km)
              
              def preprocess_cell(self, cell, resources, cell_index):
                  if cell.cell_type != 'code':
                      return cell, resources
                  
                  self.current_cell += 1
                  elapsed = time.time() - self.start_time
                  progress = (self.current_cell / self.total_cells) * 100
                  
                  print(f"\n🔄 Cell {self.current_cell}/{self.total_cells} ({progress:.1f}%) | ⏱️ {elapsed:.1f}s")
                  
                  # Show first line preview if it's meaningful
                  if cell.source:
                      first_line = cell.source.split('\n')[0][:60]
                      if first_line.strip() and not first_line.strip().startswith('#'):
                          print(f"📝 {first_line}...")
                  
                  cell_start = time.time()
                  cell, resources = super().preprocess_cell(cell, resources, cell_index)
                  cell_time = time.time() - cell_start
                  
                  if cell.outputs:
                      for output in cell.outputs:
                          if output.output_type == 'stream':
                              cleaned = self._clean_output(output.text)
                              if cleaned.strip():
                                  print(cleaned)
                          elif output.output_type == 'error':
                              print(f"❌ {output.ename}: {output.evalue}")
                  
                  print(f"✅ Completed in {cell_time:.1f}s")
                  
                  return cell, resources
              
              def _clean_output(self, text):
                  if not text:
                      return ""
                  lines = []
                  for line in text.split('\n'):
                      # Skip debug/warning lines
                      if any(skip in line for skip in ['[DEBUG]', 'WARNING:', 'DeprecationWarning']):
                          continue
                      if not line.strip():
                          continue
                      # Remove ANSI color codes
                      line = re.sub(r'\x1b\[[0-9;]*m', '', line)
                      lines.append(line)
                  return '\n'.join(lines)
          
          def run_notebook(notebook_path):
              print(f"📖 Reading: {notebook_path}")
              
              with open(notebook_path, 'r', encoding='utf-8') as f:
                  nb = nbformat.read(f, as_version=4)
              
              code_cells = sum(1 for c in nb.cells if c.cell_type == 'code')
              print(f"✅ Loaded: {len(nb.cells)} cells ({code_cells} code)")
              print()
              
              ep = ProgressTrackingExecutor(
                  timeout=2400,
                  kernel_name='python3',
                  allow_errors=False
              )
              
              print("🚀 Starting full notebook execution...")
              print()
              
              try:
                  start = time.time()
                  ep.preprocess(nb, {'metadata': {'path': '.'}})
                  duration = time.time() - start
                  
                  print()
                  print("=" * 70)
                  print("✅ FULL NOTEBOOK COMPLETED!")
                  print("=" * 70)
                  print(f"⏱️  Time: {duration:.1f}s ({duration/60:.1f} min)")
                  print(f"📊 Cells: {code_cells}")
                  print("=" * 70)
                  return True
              except Exception as e:
                  print()
                  print("=" * 70)
                  print("❌ EXECUTION FAILED!")
                  print("=" * 70)
                  print(f"Error: {type(e).__name__}: {str(e)}")
                  print("=" * 70)
                  return False
          
          if __name__ == "__main__":
              notebook = sys.argv[1] if len(sys.argv) > 1 else "AI_Forex_Brain_2.ipynb"
              
              if not os.path.exists(notebook):
                  print(f"❌ Not found: {notebook}")
                  sys.exit(1)
              
              print("=" * 70)
              print("🧠 WEEKDAY FULL NOTEBOOK - TRADE BEACON v13.0")
              print("=" * 70)
              print(f"📅 {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print(f"📓 {notebook}")
              print("🔴 Trade Beacon: Enhanced Production Edition")
              print("   • Market Regime Detection")
              print("   • Session-Based Trading")
              print("   • Momentum Confirmation")
              print("   • Strict Anti-Overfitting")
              print("=" * 70)
              print()
              
              success = run_notebook(notebook)
              
              print()
              print("=" * 70)
              if success:
                  print("✅ WEEKDAY EXECUTION COMPLETED")
              else:
                  print("❌ WEEKDAY EXECUTION FAILED")
              print(f"📅 {datetime.now().strftime('%Y-%m-%d %H:%M:%S UTC')}")
              print("=" * 70)
              
              sys.exit(0 if success else 1)
          EOFPYTHON
          
          # Find notebook file
          NOTEBOOK_FILE="AI_Forex_Brain_2.ipynb"
          
          if [ ! -f "$NOTEBOOK_FILE" ]; then
            echo "❌ Notebook not found: $NOTEBOOK_FILE"
            exit 1
          fi
          
          echo "✅ Found: $NOTEBOOK_FILE"
          echo ""
          
          python run_full_notebook.py "$NOTEBOOK_FILE" 2>&1 | tee notebook_execution.log
          
          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -ne 0 ]; then
            echo ""
            echo "❌ Notebook execution failed with exit code $EXIT_CODE"
            tail -n 200 notebook_execution.log
            exit $EXIT_CODE
          fi
          
          echo ""
          echo "✅ Notebook execution completed successfully"
        timeout-minutes: 50

      - name: Verify output files
        if: always()
        run: |
          echo "=========================================="
          echo "📋 CHECKING OUTPUT FILES"
          echo "=========================================="
          echo "Mode: Weekday Full (Trade Beacon v13.0)"
          echo ""
          
          for dir in outputs data/processed logs omega_state; do
            if [ -d "$dir" ]; then
              FILE_COUNT=$(find "$dir" -type f 2>/dev/null | wc -l)
              echo "  ✅ $dir/ ($FILE_COUNT files)"
            else
              echo "  ⚠️  $dir/ not found"
            fi
          done
          
          echo ""
          echo "Key Files:"
          for file in outputs/omega_signals.json outputs/latest_signals.json; do
            if [ -f "$file" ]; then
              SIZE=$(wc -c < "$file")
              echo "  ✅ $file ($SIZE bytes)"
            else
              echo "  ⚠️  $file missing"
            fi
          done
          
          PICKLE_COUNT=$(find data/processed -name "*.pkl" -type f 2>/dev/null | \
            grep -v "_model\|cache\|\.bak" | wc -l)
          echo ""
          echo "📦 Data Files:"
          echo "   • Processed pickles: $PICKLE_COUNT"
          
          # Check omega state
          if [ -d "omega_state" ]; then
            OMEGA_FILES=$(find omega_state -name "*.pkl" -type f 2>/dev/null | wc -l)
            echo "   • Omega state files: $OMEGA_FILES"
            
            if [ -f "omega_state/omega_learning.pkl" ]; then
              echo "   • Learning state: ✅"
            fi
            if [ -f "omega_state/omega_iteration.pkl" ]; then
              echo "   • Iteration state: ✅"
            fi
          else
            echo "   • Omega state: ⚠️  Not found"
          fi
          
          if [ $PICKLE_COUNT -ge 4 ]; then
            echo ""
            echo "✅ Data ready for Trade Beacon v13.0"
          else
            echo ""
            echo "⚠️  Insufficient data (need ≥4 pickles)"
          fi
          echo "=========================================="

      - name: Clean up nested repositories
        if: always()
        run: |
          find . -mindepth 2 -type d -name ".git" -exec rm -rf {} + 2>/dev/null || true
          [ -d "forex-alpha-models" ] && rm -rf forex-alpha-models
          echo "✅ Cleanup complete"

      - name: Commit and push changes
        if: always()
        run: |
          git add -A
          
          if git diff --cached --quiet; then
            echo "ℹ️  No changes to commit"
          else
            TIMESTAMP=$(date +'%Y-%m-%d %H:%M UTC')
            DAY=$(date +'%A')
            
            COMMIT_MSG="🔴 Trade Beacon v13.0 [4hourly] - $DAY: $TIMESTAMP"
            
            git commit -m "$COMMIT_MSG" || true
            
            echo "Syncing with remote..."
            for i in {1..3}; do
              echo "Attempt $i: Pulling..."
              if git pull --rebase origin main 2>&1 | grep -q "CONFLICT"; then
                echo "⚠️  Resolving conflict..."
                git checkout --ours .
                git add -A
                git rebase --continue || true
              fi
              
              echo "Attempt $i: Pushing..."
              if git push origin main 2>&1; then
                echo "✅ Pushed on attempt $i"
                break
              else
                echo "⚠️  Failed attempt $i"
                if [ $i -lt 3 ]; then
                  sleep 3
                  git fetch origin main
                  git reset --soft origin/main
                  git add -A
                  git commit -m "$COMMIT_MSG" || true
                fi
              fi
            done
          fi

      - name: Upload logs and artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: execution-logs-${{ github.run_number }}
          path: |
            notebook_execution.log
            run_full_notebook.py
            logs/*.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Execution Summary
        if: always()
        run: |
          echo "=========================================="
          echo "📊 EXECUTION SUMMARY"
          echo "=========================================="
          echo "Mode: ${{ steps.detect_mode.outputs.execution_mode }}"
          echo "Schedule: ${{ steps.detect_mode.outputs.schedule_type }}"
          echo "Day: $(date +'%A')"
          echo "Time: $(date +'%B %d, %Y %H:%M UTC')"
          echo ""
          
          echo "🤖 Weekday Mode (Mon-Fri):"
          echo "   • Executed: Full notebook (all cells)"
          echo "   • Pipeline: API Keys → Environment → GitHub Sync"
          echo "   • Data: Alpha Vantage + YFinance fetch"
          echo "   • Processing: CSV Combiner with indicators"
          echo "   • ML: Trade Beacon v13.0 Enhanced"
          echo "   • Next run: In ~4 hours"
          echo ""
          
          echo "Generated Files:"
          find outputs -type f -exec sh -c 'echo "  ✅ $(basename {}) ($(wc -c < {} 2>/dev/null || echo 0) bytes)"' \; 2>/dev/null | head -10
          echo ""
          
          PICKLE_COUNT=$(find data/processed -name "*.pkl" -type f 2>/dev/null | \
            grep -v "_sgd_model\|_rf_model\|indicator_cache\|ultra_\|alpha_\|_model.pkl\|\.bak" | wc -l)
          
          echo "📦 Data Files:"
          echo "   • Processed pickles: $PICKLE_COUNT"
          
          if [ -d "omega_state" ]; then
            OMEGA_LEARNING=$([ -f "omega_state/omega_learning.pkl" ] && echo "✅" || echo "⚠️")
            OMEGA_ITER=$([ -f "omega_state/omega_iteration.pkl" ] && echo "✅" || echo "⚠️")
            echo "   • Omega learning state: $OMEGA_LEARNING"
            echo "   • Omega iteration state: $OMEGA_ITER"
          else
            echo "   • Omega state: ⚠️  Not found"
          fi
          
          if [ $PICKLE_COUNT -ge 4 ]; then
            echo "   • Data Status: ✅ Ready for Trade Beacon v13.0"
          else
            echo "   • Data Status: ⚠️  Needs more data (≥4 pickles)"
          fi
          echo ""
          
          echo "🔴 TRADE BEACON v13.0 - ENHANCED PRODUCTION"
          echo "=========================================="
          echo ""
          echo "🎯 NEW FEATURES:"
          echo "   • Market Regime Detection - TRENDING/RANGING/VOLATILE"
          echo "   • Session-Based Trading - Optimal pair/session matching"
          echo "   • Momentum Confirmation - Multi-timeframe RSI analysis"
          echo "   • Enhanced Confidence - Regime + Session + Momentum weighted"
          echo ""
          echo "🛡️  VALIDATION SYSTEM:"
          echo "   • Strict validation (50% min val accuracy)"
          echo "   • Hard stops on overfitting"
          echo "   • Val/Train ratio ≥0.75 enforced"
          echo "   • Paper trading mode default"
          echo "   • No signals if all models rejected"
          echo ""
          echo "📊 MULTI-STRATEGY MODELS:"
          echo "   🔵 Conservative Trend:"
          echo "      • ATR SL: 1.5-2.0x | TP: 2.0-2.5x"
          echo "      • Risk: 0.8-1.2% per trade"
          echo "      • Confidence: 70-80% threshold"
          echo ""
          echo "   🔴 Aggressive Opportunist:"
          echo "      • ATR SL: 1.3-2.2x | TP: 1.8-3.0x"
          echo "      • Risk: 1.0-1.8% per trade"
          echo "      • Confidence: 55-70% threshold"
          echo ""
          echo "⚙️  SESSION MATRIX:"
          echo "   • USD/JPY: Asian 1.2x | European 1.0x | US 1.1x"
          echo "   • EUR/USD: Asian 0.8x | European 1.3x | US 1.2x"
          echo "   • GBP/USD: Asian 0.7x | European 1.4x | US 1.2x"
          echo "   • AUD/USD: Asian 1.3x | European 0.9x | US 1.0x"
          echo ""
          echo "🎯 SIGNAL SELECTION:"
          echo "   • Best model per pair (weighted by confidence)"
          echo "   • Regime appropriateness bonus"
          echo "   • Session optimality bonus"
          echo "   • Momentum confirmation filter"
          echo ""
          echo "📧 ENHANCED EMAIL NOTIFICATIONS:"
          echo "   • Beautiful HTML design"
          echo "   • Regime & session indicators"
          echo "   • Momentum scores displayed"
          echo "   • Model diversity visualization"
          echo "   • Trade Beacon branding"
          echo ""
          echo "🔒 SAFETY FEATURES:"
          echo "   • Dynamic seed per iteration"
          echo "   • State persistence (learning + iteration)"
          echo "   • Comprehensive logging"
          echo "   • Live price updates"
          echo "   • Browserless price fetching"
          echo "   • GitHub auto-sync"
          echo ""
          echo "=========================================="
          echo "✅ Pipeline execution completed"
          echo "=========================================="
