name: AI Forex Trading System

on:
  schedule:
    # Runs every 10 minutes, Monday–Friday
    - cron: "*/10 * * * 1-5"

  workflow_dispatch:
    inputs:
      force_run:
        description: "Force run even on weekends"
        required: false
        default: "false"

env:
  PYTHON_VERSION: "3.10"
  TZ: "UTC"

jobs:
  forex-trading:
    runs-on: ubuntu-latest

    concurrency:
      group: forex-trading
      cancel-in-progress: false

    steps:
      - name: 📅 Check trading day
        id: check_day
        run: |
          DAY=$(date +%u)
          if [[ "$DAY" -ge 6 ]]; then
            echo "weekend=true" >> $GITHUB_OUTPUT
            echo "🏖️ Weekend detected"
          else
            echo "weekend=false" >> $GITHUB_OUTPUT
            echo "✅ Trading day"
          fi

      - name: ⏭️ Skip weekend runs
        if: steps.check_day.outputs.weekend == 'true' && github.event.inputs.force_run != 'true'
        run: |
          echo "⏸️ Market closed — skipping execution"
          exit 0

      - name: 🔄 Checkout repository
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🐍 Setup Python
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: 📦 Install dependencies
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy requests yfinance python-dateutil pytz

      - name: 📂 Prepare directories
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        run: |
          mkdir -p data/raw/yfinance
          mkdir -p data/processed
          mkdir -p database
          mkdir -p logs
          mkdir -p outputs
          mkdir -p memory
          mkdir -p signal_state

      - name: 🔐 Load API keys
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        env:
          ALPHA_VANTAGE_KEY: ${{ secrets.ALPHA_VANTAGE_KEY }}
          BROWSERLESS_TOKEN: ${{ secrets.BROWSERLESS_TOKEN }}
          MARKETAUX_API_KEY: ${{ secrets.MARKETAUX_API_KEY }}
        run: |
          echo "ALPHA_VANTAGE_KEY=${ALPHA_VANTAGE_KEY}" >> $GITHUB_ENV
          echo "BROWSERLESS_TOKEN=${BROWSERLESS_TOKEN}" >> $GITHUB_ENV
          echo "MARKETAUX_API_KEY=${MARKETAUX_API_KEY}" >> $GITHUB_ENV

      - name: 📥 Restore cached state
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        uses: actions/cache@v4
        with:
          path: |
            data/
            memory/
            signal_state/
            database/
          key: forex-state-${{ github.ref_name }}
          restore-keys: |
            forex-state-

      - name: 🚀 Run trading engine
        if: steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true'
        timeout-minutes: 8
        run: |
          echo "🚀 Starting trading cycle at $(date -u)"
          python trade_beacon.py
          echo "❤️ Heartbeat: $(date -u)" >> logs/heartbeat.log

      - name: 📊 Execution summary
        if: always() && (steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true')
        run: |
          echo "==== LOG PREVIEW ===="
          [ -f logs/pipeline.log ] && tail -n 50 logs/pipeline.log
          echo ""
          echo "==== DASHBOARD STATE ===="
          [ -f signal_state/dashboard_state.json ] && head -n 40 signal_state/dashboard_state.json
          echo ""
          echo "==== API USAGE ===="
          [ -f signal_state/api_rate_limits.json ] && cat signal_state/api_rate_limits.json

      - name: 💾 Commit updated state
        if: always() && (steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true')
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add signal_state/ memory/ data/ || true

          if git diff --cached --quiet; then
            echo "No state changes to commit"
          else
            git commit -m "Auto-update trading state $(date -u '+%Y-%m-%d %H:%M UTC')"
            git push || echo "Push skipped"
          fi

      - name: 💾 Save cache
        if: always() && (steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true')
        uses: actions/cache/save@v4
        with:
          path: |
            data/
            memory/
            signal_state/
            database/
          key: forex-state-${{ github.ref_name }}

      - name: 📤 Upload logs
        if: always() && (steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true')
        uses: actions/upload-artifact@v4
        with:
          name: forex-logs-${{ github.run_number }}
          path: |
            logs/
            signal_state/
            outputs/
          retention-days: 7

      - name: 🚨 Failure notification
        if: failure()
        run: |
          echo "❌ Trading system failed"
          # Hook in Discord / Telegram / Email here

      - name: ✅ Success
        if: success() && (steps.check_day.outputs.weekend == 'false' || github.event.inputs.force_run == 'true')
        run: |
          echo "✅ Trading cycle completed successfully"
          echo "⏱ Next run in ~10 minutes"
