# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
ta>=0.11.0
scikit-learn>=1.3.0
requests>=2.31.0
yfinance>=0.2.28
python-dotenv>=1.0.0

# Sentiment Analysis Dependencies (via HuggingFace API - no local models needed)
# transformers>=4.30.0  # Not needed - using API calls
# torch>=2.0.0  # Not needed - using API calls
# sentencepiece>=0.1.99  # Not needed - using API calls

# Note: Sentiment analysis now uses HuggingFace Inference API
# No need to install heavy ML libraries locally
# Just set HF_API_KEY environment variable (optional but recommended)
