# Trade Beacon v2.1.5-FINBERT - Requirements
# ─────────────────────────────────────────────────────────────────────────────

# Core
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28

# Technical Analysis
ta>=0.11.0

# API & HTTP
requests>=2.31.0
python-dotenv>=1.0.0

# Date/Time
python-dateutil>=2.8.2

# Market Calendar
holidays>=0.35

# Machine Learning (used by performance_tracker analytics)
scikit-learn>=1.3.0

# ─────────────────────────────────────────────────────────────────────────────
# Sentiment Analysis — HuggingFace Inference API (v2.1.5-FINBERT)
# ─────────────────────────────────────────────────────────────────────────────
# FinBERT sentiment runs via HuggingFace Inference API (remote, no local GPU)
# Only requires: requests (already listed above) + HF_API_KEY secret
#
# Models used:
#   Primary:  mrm8488/distilroberta-finetuned-financial-news-sentiment-analysis
#   Fallback: ProsusAI/finbert
#
# Setup: add HF_API_KEY as a GitHub Actions secret
# Get token: https://huggingface.co/settings/tokens (free Read token)
#
# NO heavy ML libraries needed. Do NOT uncomment unless switching to local inference.
# Local inference adds ~2-4GB to the runner and ~3-5min to cold start.
#
# transformers>=4.30.0   # local inference only
# torch>=2.0.0           # local inference only
# sentencepiece>=0.1.99  # local inference only
